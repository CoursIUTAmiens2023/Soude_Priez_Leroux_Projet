package fr.soudepriezleroux.entity;

public class PacGum extends MiniCheese{

    public PacGum(String prefix, int pointsEarned, boolean isAnimated, int nbrFrame, float width, float height, float x, float y, float textureSizeX, float textureSizeY, Facing facing) {
        super(prefix, pointsEarned, isAnimated, nbrFrame, width, height, x, y, textureSizeX, textureSizeY, facing);
    }
}
